[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = tests.py test_*.py *_tests.py
addopts = -ra --cov=apps --cov=core --cov=services --cov-report=term-missing
testpaths = apps tests
