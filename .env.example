# Base URL of the Django REST API.
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Set to "true" to call the real Django backend (register/login/metrics/
# recommendations/feedback). Leave unset/false to run the frontend standalone
# in mock mode (no backend required) for a full clickable journey.
NEXT_PUBLIC_BACKEND_READY=false
